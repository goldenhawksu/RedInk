# Nixpacks 配置文件
# 强制 Railway 使用 Node.js 环境构建后端

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd backendjs && npm install"]

[phases.build]
cmds = ["cd backendjs && npm run build"]

[start]
cmd = "cd backendjs && npm start"
